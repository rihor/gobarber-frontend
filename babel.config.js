module.exports = {
  presets: [
    // responsável por alterar as funcionalidades do javascript que o navegador ainda não entende
    "@babel/preset-env",
    // responsável por alterar as funcionalidades do react que o navegador não entende, exemplo: jsx e etc...
    "@babel/preset-react"
  ]
};
